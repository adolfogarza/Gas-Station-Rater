<nav class="navbar navbar-default" role="navigation">
  <!-- Brand and toggle get grouped for better mobile display -->
  <div class="navbar-header">
    <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
      <span class="sr-only">Toggle navigation</span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
      <span class="icon-bar"></span>
    </button>
    <%= link_to t('views.navbar.brand').html_safe, root_path, class:"navbar-brand"%>
  </div>
  <% if current_user %>
  <div class="col-md-6  col-sm-offset-1">
    <%= form_tag(stations_path, :method => "get", class: "navbar-form") do %>
      <%= text_field_tag :search, params[:search], placeholder: t('views.navbar.placeholder').html_safe, class: 'form-control' %>
    <% end %>
  </div>
  <% end %>
  <!-- Collect the nav links, forms, and other content for toggling -->
  <div class="collapse navbar-collapse navbar-ex1-collapse">
    <ul class="col-md-3 nav navbar-nav navbar-right">
      <% if current_user %>
        <li><%= link_to t('views.navbar.logout').html_safe, log_out_path %></li>
      <% else %>
        <li><%= link_to t('views.navbar.sign_up').html_safe, sign_up_path %></li>
        <li><%= link_to t('views.navbar.login').html_safe, log_in_path %></li>
      <% end %>
    </ul>
  </div><!-- /.navbar-collapse -->
</nav>