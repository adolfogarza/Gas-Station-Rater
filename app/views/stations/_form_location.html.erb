<%= form_for(@station) do |f| %>
  <% if @station.errors.any? %>
    <div id="error_explanation" align="left">
      <ul>
        <% @station.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <div class="field">
    <%= f.label :legal_name, 'Razón Social de la Sucursal' %><br>
    <%= f.text_field :legal_name %>
  </div>
  <br>
  <%= f.fields_for :location do |ff| %>
    <div class="field">
      <%= ff.label :address, "Dirección de la Sucursal" %><br>
      <%= ff.text_field :address %>
    </div>
  <% end %>
  <br>
  <div class="actions">
    <%= f.button t('Dar de alta'), class: "btn btn-primary" %>
  </div>
<% end %>